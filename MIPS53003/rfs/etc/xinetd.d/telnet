service telnet 
{
        flags = REUSE IPv6
        disable = no
        socket_type = stream 
        wait = no 
        user = root 
        server = /usr/sbin/in.telnetd 
#       server_args =
        #only_from =  
}
